# GraphQL API Demo

> GraphQL server.

## Install

Clone the repository.

```sh
git clone https://github.com/stanleygomes/node-graphql.git && cd node-graphql
```

Install the dependencies.

```sh
npm install
```

Run the server

```sh
npm run dev
```
